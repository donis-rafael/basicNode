const controller = {};

const direccion = '**********/bd';
//const api = con(direccion);

controller.getConsulta1 = (req, res) => {
    
    // prepara consulta reporte
    // realizar petición a BD
    // armar objeto
    // retornar
}

controller.getConsulta2 = (req, res) => {
    
    // prepara consulta reporte
    // realizar petición a BD
    // armar objeto
    // retornar
}

controller.getConsulta3 = (req, res) => {
    
    // prepara consulta reporte
    // realizar petición a BD
    // armar objeto
    // retornar
}

module.exports = controller;
