const controller = {};
const adminService = require('../services/informes.service');

controller.getInformeDiario = async (req, res) => {

    // prepara consulta reporte
    // realizar petición a BD
    // armar objeto
    // retornar
}

controller.getInformeSemanal = async (req, res) => {

    // prepara consulta reporte
    // realizar petición a BD
    // armar objeto
    // retornar
}

controller.getInformeMensual = async (req, res) => {

    // prepara consulta reporte
    // realizar petición a BD
    // armar objeto
    // retornar
}

module.exports = controller;
