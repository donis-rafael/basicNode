const controller = {};

const direccion = '**********/bd';
//const api = con(direccion);

controller.getReporteDiario = (req, res) => {
    
    // prepara consulta reporte
    // realizar petición a BD
    // armar objeto
    // retornar
}

controller.getReporteSemanal = (req, res) => {
    
    // prepara consulta reporte
    // realizar petición a BD
    // armar objeto
    // retornar
}

controller.getReporteMensual = (req, res) => {
    
    // prepara consulta reporte
    // realizar petición a BD
    // armar objeto
    // retornar
}

module.exports = controller;
